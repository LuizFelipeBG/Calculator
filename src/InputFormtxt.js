import React, {Component} from "react"
import "./app.js"

class Transforming extends Component{

    render() {
        const initialValue = this.state.value
        const spaceRemove = initialValue.trim()
        const newArray = spaceRemove.split('+')
        return result 
        
    }
}
export default Transforming
